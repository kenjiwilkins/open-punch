# kiosk のサーバー側環境変数（値はコミットしない）。
# GRAPHQL_URL / KIOSK_API_KEY は SST の出力・Secret 由来を実行時に注入する。

# GraphQL エンドポイント（末尾に /graphql）
GRAPHQL_URL=https://xxxxx.lambda-url.ap-northeast-1.on.aws/graphql

# キオスク API キー（SST Secret: KioskApiKey）。ブラウザには出さない。
KIOSK_API_KEY=

# この端末が属する拠点 ID
KIOSK_LOCATION_ID=
