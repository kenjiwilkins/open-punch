import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // 将来 @open-punch/core / @open-punch/ui を直接 import する際に有効化する。
  // transpilePackages: ["@open-punch/core", "@open-punch/ui"],
};

export default nextConfig;
